<% @category = @gbook.categories.find(params[:id]) %>
<%= @category.name %> 
<br>
<%= "Assignments: " %> 
<br>
<% @category.assignments.each do |assignment| %>
	<%= assignment.name %> 
	<br>
<% end %>
<br>
<%= link_to 'Home Page', gbook_path(@gbook) %>