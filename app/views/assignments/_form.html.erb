<%= form_for([@gbook, @category, @assignment]) do |f| %>
  <% if @assignment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@assignment.errors.count, "error") %> prohibited this assignment from being saved:</h2>

      <ul>
      <% @assignment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :category_id %><br>
    <%= f.select :category_id, [[@gbook.categories.first,@gbook.categories.first],[@gbook.categories.second,@gbook.categories.second],[@gbook.categories.last,@gbook.categories.last]] %>
  </div>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :point_total %><br>
    <%= f.text_field :point_total %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
